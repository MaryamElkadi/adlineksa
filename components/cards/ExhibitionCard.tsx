import Link from "next/link"; import Image from "next/image";
import { Exhibition } from "@/types";
import { formatCurrency } from "@/lib/utils";
export function ExhibitionCard({ exhibition }: { exhibition: Exhibition }) { const date = exhibition.date ? new Intl.DateTimeFormat("ar-SA", { dateStyle: "medium" }).format(new Date(exhibition.date)) : ""; return <article dir="rtl" className="group flex h-full flex-col overflow-hidden rounded-3xl border border-slate-200 bg-white shadow-sm transition-all duration-300 hover:-translate-y-1 hover:border-amber-300 hover:shadow-xl"><div className="relative aspect-[16/10] overflow-hidden bg-slate-100"><Image src={exhibition.image || "/products/printing.png"} alt={exhibition.titleAr} fill sizes="(max-width: 640px) 100vw, (max-width: 1024px) 50vw, 33vw" quality={72} className="object-cover transition-transform duration-500 group-hover:scale-105" />{exhibition.featured && <span className="absolute right-3 top-3 rounded-full bg-amber-400 px-3 py-1 text-[11px] font-black text-slate-900">مميز</span>}</div><div className="flex flex-1 flex-col p-5"><span className="text-[11px] font-black text-amber-700">{exhibition.categoryAr || exhibition.category}</span><h3 className="mt-2 text-lg font-black text-brand-blue">{exhibition.titleAr || exhibition.title}</h3><p className="mt-2 line-clamp-2 text-sm leading-6 text-slate-600">{exhibition.shortDescriptionAr || exhibition.descriptionAr}</p>{(exhibition.locationAr || date) && <p className="mt-3 text-xs font-semibold text-slate-500">{[exhibition.locationAr || exhibition.location, date].filter(Boolean).join(" • ")}</p>}<div className="mt-auto flex items-center justify-between gap-3 border-t border-slate-100 pt-4"><span className="text-xs font-black text-brand-blue">{exhibition.price !== undefined ? `يبدأ من ${formatCurrency(exhibition.price)}` : exhibition.priceLabel || "اطلب عرض سعر"}</span><Link href={`/exhibitions/${exhibition.slug}`} className="rounded-xl bg-slate-900 px-3 py-2 text-xs font-bold text-white hover:bg-amber-400 hover:text-slate-900">عرض التفاصيل</Link></div></div></article>; }
